import {bilanReadiness} from '../lib/readiness';import type{DrillItem}from'../lib/readiness';
interface ReadinessMeterProps{items:DrillItem[];}
export default function ReadinessMeter({items}:ReadinessMeterProps){const {pct: score}=bilanReadiness(items);const color=score>=80?'bg-green-500':score>=50?'bg-yellow-500':'bg-red-500';const label=score>=80?'Ready!':score>=50?'Getting there':'Needs review';return(<div className='bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-4'><div className='flex justify-between items-center mb-2'><span className='text-sm font-medium text-gray-700 dark:text-gray-300'>Readiness</span><span className={'text-xs px-2 py-0.5 rounded-full text-white '+color}>{label}</span></div><div className='w-full bg-gray-200 rounded-full h-3 dark:bg-gray-700'><div className={'h-3 rounded-full transition-all duration-500 '+color} style={{width:score+'%'}}/></div><p className='text-xs text-gray-400 mt-1 dark:text-gray-500'>{score}% ready - {items.length} items tracked</p></div>);
}
